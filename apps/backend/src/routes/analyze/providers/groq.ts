/**
 * Sprint 2 placeholder — Groq provider execution.
 *
 * Planned interface:
 *   executeReview(ctx: AnalyzeContext): Promise<ReviewOutput>
 *   executeRewrite(ctx: AnalyzeContext, reviewOutput: ReviewOutput): Promise<string>
 *   executeSeo(ctx: AnalyzeContext, article: string): Promise<Record<string, unknown>>
 *
 * Currently the Groq execution lives in handlers/analyze.ts (OpenAI-compatible branch).
 * This file is reserved for Sprint 2 provider abstraction.
 */
